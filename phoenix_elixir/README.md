= phoenix

```
$ sudo apt install elixir

$ mix local.hex
$ mix archive.install hex phx_new

$ sudo apt install inotify-tools

$ docker run --name phoenix1 -e POSTGRES_PASSWORD=*** -d postgres

$ mix phx.new <helloworld>

$ cd <helloworld>

$ sudo apt install erlang-dev
$ vim config/dev.exs

$ mix ecto.create

$ sudo apt install erlang-xmerl
$ mix phx.server
```
